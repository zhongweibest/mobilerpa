{
  "name": "mobilerpa-script-monorepo",
  "private": true,
  "version": "0.1.0",
  "devDependencies": {
    "esbuild": "^0.25.9",
    "typescript": "^5.6.3"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "check": "npm run check --workspaces --if-present",
    "release": "node tools/release-all.js"
  }
}

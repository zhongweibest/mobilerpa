# Agent 与脚本架构

## 分层结构
- `agent/`：手机端常驻入口
- `scripts/<name>/src/`：可复用的业务逻辑
- `scripts/<name>/debug/`：仅供开发阶段使用的调试入口
- `tools/`：本地机器上的真机快速调试工具

## 运行模式
1. 本地快速调试模式
2. 平台联调模式
3. 生产模式

三种模式必须共享同一套脚本核心逻辑，只在入口和运行时装配上有所区别。

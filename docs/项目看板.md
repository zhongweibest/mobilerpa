# MobileRPA 项目看板

## 1. 看板使用方式

本文档是 MobileRPA 当前执行状态、任务完成情况和下一步优先级的唯一维护入口。

看板按三层维护：

1. 开发阶段：例如 `S1` 中心接入主链路。
2. 开发周期：例如 `C1-1` WebSocket `hello/heartbeat` 链路。
3. 开发任务：例如 `P1-BE-006` 完成 WebSocket `hello` 手工验收脚本。

验收文档选择规则：

1. 每个需要人工验收的开发周期，必须在周期目标后写明“验收文档”。
2. 当前任务验收时，优先使用看板中该周期标注的验收文档。
3. 如果看板中没有标注，使用 `docs/acceptance-checklists/README.md` 的验收入口索引。
4. 如果标注为“待实现后生成”，说明当前功能还没有真实可执行验收文档，不能使用归档草稿替代验收。
5. 如果两处都没有对应文档，需要先补充或确认模块验收文档，不能临时猜测使用哪份文档。

## 2. 状态标记规则

- `[ ]`：未开始
- `[~]`：进行中
- `[?]`：等待用户验收
- `[x]`：已完成
- `[!]`：阻塞中

## 3. 状态维护硬约束

1. 任务、周期、阶段都必须有独立状态。
2. 我可以在实际开始开发某个任务时，把任务状态从 `[ ]` 改为 `[~]`。
3. 当我完成实现、联调或验证准备后，只能把任务状态改为 `[?]`，表示等待用户验收。
4. 只有用户明确确认“验收通过”“确认完成”或表达等价含义后，我才能把任务、周期或阶段状态改为 `[x]`。
5. 不能仅凭代码写完、测试通过、脚本跑通或我自己的判断，就把任务、周期或阶段标记为 `[x]`。
6. 一个开发周期内的必须任务全部 `[x]` 后，还需要用户明确确认该周期验收通过，才能把周期标记为 `[x]`。
7. 一个开发阶段内的必须周期全部 `[x]` 后，还需要用户明确确认该阶段验收通过，才能把阶段标记为 `[x]`。
8. 进入下一个开发周期或开发阶段前，需要先完成当前周期或阶段验收，除非用户明确同意并行推进或调整顺序。
9. 如果我认为计划顺序需要调整，只能先提出建议和理由，必须经过用户同意后才能修改计划或改变推进顺序。
10. 默认文档和代码注释都使用中文。
11. 如果我有疑问、觉得要求不合理或发现隐藏风险，必须先停下来说明判断、建议和可选方案，等待用户确认后再执行。
12. 涉及工程惯例的文件名或入口名，例如 `README.md`、配置文件、启动入口、目录结构，不允许我直接按自己的偏好改动，必须先让用户确认。

## 4. 责任角色

- 后端：中心服务、数据库、HTTP、WebSocket、任务调度、文件归档
- Agent/脚本：AutoJs Agent、业务脚本、调试入口、真机联调
- 前端：Vue 管理后台、页面交互、接口联调
- 验收：人工功能验证、试运行记录、问题回归

## 5. 当前状态总览

| 层级 | 已完成 | 进行中 | 等待验收 | 阻塞中 | 未开始 |
|---|---:|---:|---:|---:|---:|
| 开发阶段 | 1 | 2 | 0 | 0 | 3 |
| 开发周期 | 8 | 1 | 0 | 0 | 11 |
| 开发任务 | 49 | 3 | 0 | 0 | 44 |

当前建议推进：你已确认正式采用“计划任务层”作为统一调度层。下一步先完成 `C2-6` 的文档收口，再先搭计划任务基础设施层，然后按“单脚本执行链迁移 -> 工作流执行链迁移”的顺序推进，最终把原工作流实例层从主入口移除。

## 6. `[~]` S0：项目初始化与文档基线

阶段目标：完成双仓结构、开发规范、中文文档规则、中心服务最小 HTTP 验收链路。

阶段状态说明：阶段内主要任务已完成，但本阶段尚未由用户明确进行阶段级验收确认，所以阶段状态暂不标记为 `[x]`。

### `[x]` C0-1：仓库与规范初始化

周期目标：初始化双仓库、补齐忽略规则、完成首次提交并确认中文默认规则。

- [x] `DONE-001` 规划双仓结构。角色：后端。验收：确认 `mobilerpa-center` 和 `mobilerpa-agent` 拆分方案。
- [x] `DONE-002` 初始化中心仓库。角色：后端。验收：`repos/mobilerpa-center` 已执行 `git init`。
- [x] `DONE-003` 初始化 Agent 仓库。角色：Agent/脚本。验收：`repos/mobilerpa-agent` 已执行 `git init`。
- [x] `DONE-004` 完成两个仓库首次提交。角色：后端。验收：中心仓库提交 `b5f3b95`，Agent 仓库提交 `1245c36`。
- [x] `DONE-005` 完成中心端 `.gitignore`。角色：后端。验收：中心仓库已忽略构建产物、临时文件和前端依赖。
- [x] `DONE-006` 完成 Agent 端 `.gitignore`。角色：Agent/脚本。验收：Agent 仓库已忽略运行输出、本地状态和工具产物。
- [x] `DONE-010` 统一中文文档和中文代码注释规则。角色：验收。验收：开发规范已写入中文默认规则，中心端 Go 注释已改为中文。

周期验收：用户已确认文档和注释使用中文，并已继续推进后续工作。

### `[x]` C0-2：中心 HTTP 最小验收链路

周期目标：中心服务可通过环境变量或 `.env` 默认配置启动，设备可注册，设备列表和单设备详情可查询，并可通过真实 API 调用验收。

验收文档：`repos/mobilerpa-center/docs/server/中心服务手工验收.md`。

- [x] `P1-BE-001` 建立中心服务启动入口。角色：后端。验收：`go run .\cmd\center` 可启动服务。
- [x] `P1-BE-002` 建立 SQLite 初始化逻辑。角色：后端。验收：启动服务后可自动创建数据库表。
- [x] `P1-BE-002A` 支持 `.env` 默认配置启动中心服务。角色：后端。验收：不手动设置 `$env:CENTER_HTTP_ADDR` 和 `$env:CENTER_DB_PATH` 时，`go run .\cmd\center` 能读取 `.env` 默认配置；手动环境变量仍可覆盖 `.env`。
- [x] `P1-BE-003` 实现设备注册接口。角色：后端。验收：Apifox 或脚本调用 `POST /api/v1/device/register` 返回 `status=ok`。
- [x] `P1-BE-004` 实现设备列表接口。角色：后端。验收：Apifox 或脚本调用 `GET /api/v1/devices` 能看到注册设备。
- [x] `P1-BE-004A` 实现单设备查询接口。角色：后端。验收：Apifox 或脚本调用 `GET /api/v1/devices/{deviceId}` 能看到指定设备，不存在时返回 `404 device_not_found`。
- [x] `P1-BE-005` 建立真实 API 冒烟验收脚本。角色：后端。验收：`powershell -ExecutionPolicy Bypass -File .\tools\api-smoke-test.ps1` 通过。
- [x] `P1-QA-001` 完成 HTTP 接口冒烟验收。角色：验收。验收：健康检查、注册、列表查询全部通过。
- [x] `DONE-007` 完成中心端 SQLite 初始化和设备注册接口。角色：后端。验收：注册接口可生成 `device_id`。
- [x] `DONE-008` 完成中心端设备列表接口。角色：后端。验收：`GET /api/v1/devices` 可返回已注册设备。
- [x] `DONE-009` 完成真实启动服务和 API 验收脚本。角色：后端。验收：`tools/api-smoke-test.ps1` 可跑通健康检查、注册、列表查询。

周期验收：用户已通过启动服务方式完成历史 HTTP 主链路验收，并已明确确认 `P1-BE-002A` 和 `P1-BE-004A` 增项验收完成。

## 7. `[x]` S1：中心接入主链路

阶段目标：设备可以注册，中心可以识别设备，并能通过 WebSocket 维护基础在线状态。
阶段验收：你已明确确认 “看起来 S1 都完成了”，因此本阶段正式收口。

### `[x]` C1-1：WebSocket `hello/heartbeat` 链路

周期目标：模拟客户端可以连接 `/ws`，发送 `hello` 和 `heartbeat`，中心可以维护在线状态。

验收文档：`repos/mobilerpa-center/docs/server/中心服务手工验收.md`。

- [x] `P1-BE-006` 完成 WebSocket `hello` 手工验收脚本。角色：后端。验收：脚本连接 `/ws` 后发送 `hello`，中心返回 `ack=ok`。
- [x] `P1-BE-007` 完成 WebSocket `heartbeat` 手工验收脚本。角色：后端。验收：脚本发送 `heartbeat` 后设备状态变为 `online`。
- [x] `P1-BE-008` 增加设备离线判定策略。角色：后端。验收：断开 WebSocket 后设备状态能在合理时间内变为 `offline`。
- [x] `P1-QA-002` 完成 WebSocket 上线验收。角色：验收。验收：设备或模拟器发送 `hello` 后中心记录在线。

周期验收：用户已明确确认 `hello/heartbeat` 功能验收通过。

### `[x]` C1-1A：心跳超时自动离线增强

周期目标：中心在 WebSocket 未显式断开时，也能根据最近心跳时间自动回收设备在线状态，并把相关阈值纳入启动配置。

验收文档：待实现后补充到 `repos/mobilerpa-center/docs/server/中心服务手工验收.md`；当前辅助文档：`docs/整体规划方案.md`、`docs/开发阶段计划.md`。

- [x] `P1-BE-008A` 实现心跳超时自动离线扫描。角色：后端。验收：设备停止心跳且超过 `offline timeout` 后，即使 WebSocket 未及时触发断开，中心也会把设备状态改为 `offline`。
- [x] `P1-BE-008B` 支持 `CENTER_HEARTBEAT_INTERVAL`、`CENTER_DEVICE_OFFLINE_TIMEOUT`、`CENTER_DEVICE_OFFLINE_SCAN_INTERVAL` 启动配置。角色：后端。验收：中心启动后可从 `.env` 或环境变量读取三个配置项，且环境变量优先级更高。
- [x] `P1-QA-002A` 完成心跳超时自动离线人工验收文档。角色：验收。验收：文档中提供可直接复制到 Apifox、PowerShell 或等价工具的完整步骤，覆盖正常在线、停止心跳、自动离线和参数调整验证。

周期验收：用户已明确确认 `C1-1A` 验收完成。

### `[x]` C1-2：Agent 注册与心跳

周期目标：Agent 可启动、注册、保存身份、连接 WebSocket 并发送心跳。

验收文档：`repos/mobilerpa-agent/docs/手机端手工验收.md`；当前辅助文档：`repos/mobilerpa-agent/docs/手机端运行时最小契约.md`。

- [x] `P1-AG-001` 建立 `agent.js` 主入口。角色：Agent/脚本。验收：AutoJs6 可执行 Agent 主入口并输出启动日志。
- [x] `P1-AG-002` 实现本地配置读写。角色：Agent/脚本。验收：Agent 可读取和写入本地 `config.json`。
- [x] `P1-AG-003` 实现 `agent_uuid` 生成和持久化。角色：Agent/脚本。验收：重启 Agent 后 `agent_uuid` 不变。
- [x] `P1-AG-004` 实现中心注册调用。角色：Agent/脚本。验收：Agent 调用中心注册接口并保存 `device_id`。
- [x] `P1-AG-005` 实现 WebSocket 连接。角色：Agent/脚本。验收：Agent 能连接中心 `/ws`。
- [x] `P1-AG-006` 实现心跳发送。角色：Agent/脚本。验收：中心能收到设备心跳并更新在线状态。
- [x] `P1-AG-006A` 实现 WebSocket 自动重连。角色：Agent/脚本。验收：中心服务中断后，Agent 按 `3s -> 6s -> 12s -> 24s -> 48s -> 60s -> 60s ...` 无限次尝试重连，中心恢复后自动重新上线并恢复心跳。
- [x] `P1-AG-007` 增强调试推送脚本，支持 `all` 批量推送。角色：Agent/脚本。验收：`push-agent.bat` 可对当前 ADB 已连接且已授权的全部真机完成一次批量推送。

周期验收：用户已明确确认 `C1-2` 验收完成。

### `[x]` C1-3：设备列表前端入口

周期目标：设备列表页面可以展示真实接口数据和关键状态字段，并支持清理离线历史设备。

验收文档：`repos/mobilerpa-center/docs/web/前端手工验收.md`；当前辅助文档：`repos/mobilerpa-center/docs/web/前端结构说明.md`。

- [x] `P1-FE-001` 确认前端工程技术栈。角色：前端。验收：输出前端工程结构和依赖清单。
- [x] `P1-FE-002` 搭建设备页骨架。角色：前端。验收：页面能展示设备列表占位数据。
- [x] `P1-FE-003` 接入设备列表接口。角色：前端。验收：页面能展示 `GET /api/v1/devices` 返回的数据。
- [x] `P1-FE-004` 显示设备注册状态。角色：前端。验收：页面能显示 `status`、`bind_status`、`last_heartbeat_at`。
- [x] `P1-BE-004B` 实现离线设备删除接口。角色：后端。验收：`DELETE /api/v1/devices/{deviceId}` 可删除离线设备，在线设备返回冲突错误。
- [x] `P1-FE-004A` 在设备列表页增加删除入口。角色：前端。验收：页面可删除离线设备，删除后刷新列表不再显示该设备。
- [x] `P1-QA-004A` 完成设备删除人工验收。角色：验收。验收：用户可通过前端、PowerShell 或 Apifox 删除离线设备，并确认在线设备不能误删。

周期验收：用户已确认页面可展示 `status`、`bind_status`、`last_heartbeat_at`，刷新按钮可用，且本地联调无跨域问题。

### `[x]` C1-4：阶段验收与接口集合

周期目标：补齐接口集合或等价 API 说明，并形成 S1 阶段验收结论。

验收文档：`docs/验收记录/S1-Apifox接口集合说明.md`、`docs/验收记录/S1阶段验收记录.md`；当前辅助文档：`docs/项目看板.md`、`docs/开发阶段计划.md`。

- [x] `P1-QA-003` 编写 Apifox 接口集合。角色：验收。验收：Apifox 中包含健康检查、设备注册、设备列表、WebSocket 说明。
- [x] `P1-QA-004` 记录第 1 阶段验收结论。角色：验收。验收：在验收记录中写明通过项、问题项和下一步。

周期验收门禁：用户确认 S1 阶段验收记录并明确同意进入 S2 后，才能把 S1 阶段标记为 `[x]`。

## 8. `[ ]` S2：单任务下发与脚本执行

阶段目标：中心可以向单台设备下发任务，Agent 可以拉起脚本并回传执行结果；同时补齐脚本版本管理、脚本独立下发和任务中心选择脚本版本能力。

### `[x]` C2-1：任务模型与中心接口

验收文档：待实现后补充到 `repos/mobilerpa-center/docs/server/中心服务手工验收.md`。

- [x] `P2-BE-001` 定义任务状态枚举。角色：后端。验收：文档和代码中明确 `pending`、`assigned`、`running`、`success`、`failed`。
- [x] `P2-BE-002` 实现任务创建接口。角色：后端。验收：`POST /api/v1/tasks` 可创建任务。
- [x] `P2-BE-003` 实现任务列表接口。角色：后端。验收：`GET /api/v1/tasks` 可查询任务。
- [x] `P2-BE-003A` 实现任务事件查询接口。角色：后端。验收：`GET /api/v1/tasks/{taskId}/events` 可查询任务事件，便于通过 Apifox 验收事件记录。
- [x] `P2-BE-007` 实现任务事件记录。角色：后端。验收：任务关键事件写入 `task_events`。

周期验收门禁：用户通过 API 工具确认任务创建、查询和事件记录可用。

周期验收：用户已明确确认任务创建、任务查询、任务事件查询通过验收，并同意继续向下推进。
### `[x]` C2-2：任务下发与回执

验收文档：`repos/mobilerpa-center/docs/server/中心服务手工验收.md` 的 `8B. C2-2 任务下发与回执手工验收`，以及 `repos/mobilerpa-agent/docs/手机端手工验收.md` 中与 `assign_task/task_ack` 对应的日志观察步骤。

- [x] `P2-BE-004` 实现单设备任务下发。角色：后端。验收：中心可通过 WebSocket 向指定设备发送 `assign_task`。
- [x] `P2-BE-005` 实现 `task_ack` 处理。角色：后端。验收：设备确认接收任务后任务状态更新。
- [x] `P2-AG-001` 实现任务消息分发。角色：Agent/脚本。验收：Agent 能识别 `assign_task`。
- [x] `P2-AG-002` 实现任务确认回传。角色：Agent/脚本。验收：Agent 收到任务后发送 `task_ack`。
- [x] `P2-QA-001` 完成单设备任务下发验收。角色：验收。验收：中心下发任务，设备收到任务。

周期验收门禁：用户确认中心下发任务后，设备或模拟 Agent 能收到并确认。

### `[x]` C2-3：脚本执行与结果回传

验收文档：`repos/mobilerpa-agent/docs/手机端手工验收.md` 的 `7B. C2-3 脚本执行与 task_result 验收`，以及 `repos/mobilerpa-center/docs/server/中心服务手工验收.md` 的 `8C. C2-3 脚本执行与结果回传手工验收`。

- [x] `P2-AG-003` 实现脚本执行入口。角色：Agent/脚本。验收：当前先以内置 `shoppe_sync` 演示执行器打通最小执行闭环。
- [x] `P2-AG-004` 实现执行结果回传。角色：Agent/脚本。验收：脚本结束后 Agent 发送 `task_result`。
- [x] `P2-AG-005` 迁移 `shoppe` 第一版入口。角色：Agent/脚本。验收：`shoppe_sync@v0.1.0` 已改为真实脚本文件入口 `agent/scripts/shoppe_sync/v0.1.0/index.js` 并可经 Agent 执行。
- [x] `P2-BE-009` 实现脚本元数据与下载接口。角色：后端。验收：`GET /api/v1/script/manifest` 和 `GET /api/v1/script/download` 可返回指定脚本版本。
- [x] `P2-AG-007` 实现 Agent 缺失脚本时自动下载。角色：Agent/脚本。验收：删除真机本地脚本目录后，下发任务可触发下载并执行。
- [x] `P2-BE-010` 实现脚本完整文件清单接口能力。角色：后端。验收：`manifest` 可返回版本目录文件清单，`download` 可按相对路径下载脚本文件。
- [x] `P2-AG-008` 实现 Agent 按文件清单同步完整脚本目录。角色：Agent/脚本。验收：删除真机本地脚本版本目录后，下发任务可下载 `index.js`、`core/`、`utils/`、`config/` 等完整目录。
- [x] `P2-AG-009` 为脚本版本提供真机直跑调试入口。角色：Agent/脚本。验收：可在 AutoJs6 中直接运行 `index_debug.js` 调试脚本，不依赖中心服务和 Agent 下发。
- [x] `P2-AG-006` 增加关键步骤事件上报。角色：Agent/脚本。验收：单个任务执行中至少上报 `INIT_APP`、`CHECK_HOME`、`COMPLETE` 等关键步骤事件；中心服务可存储；后台可查看。不做百分比进度，也不做细粒度实时流式日志。
- [x] `P2-BE-006` 实现 `task_result` 处理。角色：后端。验收：设备回传结果后任务状态变为成功或失败。
- [x] `P2-QA-002` 完成脚本执行验收。角色：验收。验收：设备拉起真实脚本文件入口并产生日志。
- [x] `P2-QA-003` 完成结果回传验收。角色：验收。验收：中心能看到成功或失败结果，并记录 `task_running`、`task_result` 事件。

周期验收：你已明确确认 `C2-3` 验收通过。

周期验收门禁：用户在真机或接近真实环境中确认脚本被拉起并回传结果。

### `[x]` C2-4：任务页最小入口

验收文档：待实现后生成 `repos/mobilerpa-center/docs/web/前端手工验收.md`。

- [x] `P2-FE-001` 搭建任务页骨架。角色：前端。验收：页面能展示任务列表占位数据。
- [x] `P2-FE-002` 接入任务列表接口。角色：前端。验收：页面能展示真实任务列表。
- [x] `P2-FE-003` 实现创建任务表单。角色：前端。验收：页面可创建单设备任务。
- [x] `P2-FE-004` 展示任务状态。角色：前端。验收：页面能展示任务当前状态、设备、脚本名称和更新时间。
- [x] `P2-FE-006` 实现任务下发按钮。角色：前端。验收：`pending` 任务可从页面点击下发，成功后状态更新。
- [x] `P2-QA-004` 记录第 2 阶段验收结论。角色：验收。验收：记录通过项、失败样例和修复计划。

周期验收：你已明确确认 `C2-4` 验收通过。

周期验收门禁：用户确认可以通过后台页面或 API 工具完成单任务创建和状态查看。

### `[x]` C2-5：脚本管理优先闭环

验收文档：待实现后补充到 `repos/mobilerpa-center/docs/server/中心服务手工验收.md`、`repos/mobilerpa-center/docs/web/前端手工验收.md` 和 `repos/mobilerpa-agent/docs/手机端手工验收.md`。

- [x] `P2-SC-001` 设计脚本管理最小数据模型和接口边界。角色：后端。验收：明确脚本列表、版本列表、版本详情、上传、独立下发和任务引用关系。
- [x] `P2-SC-002` 实现脚本上传入库接口，支持 `zip` 上传。角色：后端。验收：中心可接收脚本版本上传请求。
- [x] `P2-SC-003` 实现中心脚本库落盘与版本目录整理逻辑。角色：后端。验收：中心将上传内容统一保存为已解压版本目录，不再依赖 `mobilerpa-agent` 仓库路径。
- [x] `P2-SC-004` 实现脚本列表与版本列表接口。角色：后端。验收：可查询当前中心维护的脚本及版本。
- [x] `P2-SC-005` 实现脚本版本详情接口。角色：后端。验收：可查看入口文件、文件清单、来源类型和下载信息。
- [x] `P2-SC-006` 实现脚本版本独立下发到设备接口。角色：后端。验收：不创建任务也可把指定脚本版本同步到手机。
- [x] `P2-SC-007` 新增“脚本管理”前端菜单和页面骨架。角色：前端。验收：左侧菜单出现“脚本管理”，页面可进入。
- [x] `P2-SC-008` 展示脚本版本列表、文件清单、来源类型和调试入口说明。角色：前端。验收：页面可看到 `shoppe_sync@v0.1.1` 及其文件清单。
- [x] `P2-SC-009` 前端支持上传脚本版本。角色：前端。验收：页面可上传 `zip` 脚本版本。
- [x] `P2-SC-010` 前端支持选择设备并下发脚本版本。角色：前端。验收：页面可选择设备并触发脚本独立下发。
- [x] `P2-SC-011` 改造任务创建表单以选择脚本版本。角色：前端。验收：创建任务时从脚本版本列表选择 `script_name` 和 `script_version`。
- [x] `P2-SC-012` 补充脚本管理人工验收文档。角色：验收。验收：文档覆盖上传、脚本列表、版本详情、独立下发、手机目录检查和任务中心选择脚本版本。
- [x] `P2-SC-014` 支持删除单个脚本版本。角色：后端/前端。验收：脚本管理页可删除指定 `script_name + script_version`，删除后该版本从列表消失、版本详情接口返回 `404 script_version_not_found`，中心脚本目录对应版本被移除。
- [x] `P2-SC-015` 支持按脚本名删除全部版本。角色：后端/前端。验收：脚本管理页可删除指定 `script_name`，删除后该脚本整组从列表消失、该脚本名下全部版本不可再查询，中心脚本目录对应脚本根目录被移除。
周期验收门禁：用户确认中心服务可独立维护脚本库，脚本管理页可上传并查看脚本版本，可独立下发脚本到手机，并可在任务中心选择脚本版本创建任务。

周期验收：你已明确确认 `C2-2`、`C2-3`、`C2-4`、`C3-1B`、设备删除增项通过验收，并确认脚本管理相关功能已可用；同时确认 `index_debug.js` 调试入口可作为 `P2-AG-009` 的验收结果。`P2-SC-013` 已按你的要求后移到整个项目后期实现，不再作为本周期收口条件。

### `[ ]` C2-6：计划任务调度层与执行域迁移基础

验收文档：当前方案文档为 [整体规划方案](D:/dev/code/mobilerpa/docs/整体规划方案.md) 中新的“14. 计划任务调度层规划”章节；真实手工验收文档待实现后生成。

- [ ] `P2-PL-001` 定义计划任务主模型。角色：后端。验收：文档和实现中已明确 `plan_def`、`plan_runs`、`plan_device_runs`、`plan_events` 等核心对象。
- [ ] `P2-PL-002` 设计计划任务目标类型模型。角色：后端。验收：计划任务第一版同时支持 `script` 和 `workflow` 两类 `target_type`。
- [ ] `P2-PL-003` 设计计划任务时间规则。角色：后端。验收：时间规则统一收口到计划任务层，包含每日开始时间、每日截止时间、按天循环和启停状态。
- [ ] `P2-PL-004` 设计计划任务设备运行模型。角色：后端。验收：同一计划任务一次启动只创建一个计划任务运行实例，实例下挂多台设备运行记录。
- [ ] `P2-PL-005` 设计运行中添加设备、删除设备、停止单设备、停止整个计划任务运行实例的统一语义。角色：后端。验收：添加设备会立即拉起执行，删除设备会立即停止执行。
- [ ] `P2-PL-006` 设计设备占用归口规则。角色：后端。验收：设备占用默认由计划任务运行域裁决，手工任务只保留兼容入口。
- [ ] `P2-PL-007` 设计从旧工作流实例层迁移到计划任务实例层的策略。角色：后端/前端。验收：已明确旧工作流实例页、接口和数据模型的移除或兼容范围。
- [ ] `P2-PL-008` 设计中心与 Agent 的职责边界。角色：后端/Agent。验收：计划任务层负责调度裁决，Agent 负责目标执行和关键事件回传。
- [ ] `P2-PL-009` 设计 300 到 500 台设备规模下的计划任务事件收敛、查询分层和 WebSocket 主题拆分策略。角色：后端/前端。验收：已明确计划任务实例、设备运行记录和消息订阅的主题边界。
- [ ] `P2-PL-010` 先构建计划任务基础设施层。角色：后端。验收：数据库表结构、调度扫描器、最小后端服务和统一运行事件模型可用。
- [ ] `P2-PL-011` 将“单脚本+版本”的执行链迁移到计划任务调度层。角色：后端/Agent。验收：可通过计划任务直接调度单脚本版本执行。
- [ ] `P2-PL-012` 将工作流执行链迁移到计划任务调度层。角色：后端/Agent。验收：可通过计划任务直接调度工作流定义执行。
- [ ] `P2-PL-013` 新增“计划任务”菜单和“计划任务实例”菜单。角色：前端。验收：可定义计划任务、查看实例、运行中加减设备和停止控制。
- [ ] `P2-PL-014` 收口原“工作流实例”页面和接口。角色：前端/后端。验收：迁移完成后，运行态统一从“计划任务实例”入口查看。

周期验收门禁：用户已明确确认采用“计划任务层”作为正式调度模型；后续实现按“计划任务基础设施层 -> 单脚本执行链迁移 -> 工作流执行链迁移”的顺序推进。

当前阶段验收焦点：

- [ ] `PLAN-UI-001` 验证计划任务定义页可配置目标类型、目标对象、设备集合、开始时间、截止时间和循环方式。角色：验收。验收：可创建一条调度单脚本或工作流的计划任务。
- [ ] `PLAN-UI-002` 验证计划任务实例页可查看实例、查看设备运行记录、查看事件并执行停止控制。角色：验收。验收：实例页成为统一运行态入口。
- [ ] `PLAN-UI-003` 验证计划任务运行中可添加设备和删除设备。角色：验收。验收：添加设备后立即执行，删除设备后立即停止。

### `[~]` C2-7：工作流实例层与手工任务移除、执行域合并

验收文档：方案见 [整体规划方案 §14.15](D:/dev/code/mobilerpa/docs/整体规划方案.md)、[中心服务架构·工作流实例层移除](D:/dev/code/mobilerpa/repos/mobilerpa-center/docs/server/中心服务架构.md)、[迁移说明](D:/dev/code/mobilerpa/repos/mobilerpa-center/docs/server/工作流实例层移除迁移说明.md)；真实手工验收文档待实现后生成。

- [ ] `P2-WI-001` 删除 `workflow_instances`/`workflow_runs`/`workflow_contexts`/`workflow_events` 表与代码，保留 `workflow_defs`/`workflow_nodes`/`workflow_edges`。角色：后端。验收：四张实例表与对应代码消失，定义表保留。
- [ ] `P2-WI-002` `plan_runs.target_ref_id` 工作流型指向 `workflow_def_id`；`plan_device_runs` 新增工作流投影列。角色：后端。验收：工作流型计划任务实例按定义引用，设备运行记录可见当前节点。
- [ ] `P2-WI-003` `tasks` 表删除 `workflow_*` 列，移除“中心逐节点 task 推进”链路。角色：后端。验收：`startRunFromNode`/`advanceRunAfterNode` 等消失，`task_source_type` 仅余 `manual`/`plan_script`。
- [ ] `P2-WI-004` `internal/workflow` 收敛为定义+快照构建，运行态归入 `internal/plan`。角色：后端。验收：工作流型计划任务启动、节点推进、停止由 plan 域闭环。
- [ ] `P2-WI-005` 会话载荷与 Agent checkpoint 改用 `plan_run_id`+`plan_device_run_id`，删除短期兼容。角色：后端/Agent。验收：Agent 收到的会话载荷与本地 checkpoint 均以这两键定位。
- [ ] `P2-WI-006` 前端删除“工作流实例”页，计划任务实例页展示工作流节点与事件。角色：前端。验收：菜单无工作流实例入口，节点事件在计划任务实例页可见。
- [ ] `P2-WI-007` 移除“脚本型补查 `workflow_runs`”临时实现，占用判定回归 `plan_device_runs` 单域。角色：后端。验收：脚本型与工作流型并存占用裁决一致。
- [?] `P2-WI-008` 删除手工任务 HTTP 入口（`POST/PATCH /api/v1/tasks`、`/terminate`、`/devices/{id}/manual-tasks/*`）与 `task.StopManualTask`。角色：后端。验收：手工任务相关接口全部不可用或移除。
- [?] `P2-WI-009` 清理 `task_source_type='manual'` 与各服务手工任务占用分支；`task_source_type` 仅保留 `plan_script`。角色：后端。验收：占用判定仅依赖 `plan_device_runs`。
- [?] `P2-WI-010` 确认前端无任务中心 API 残留。角色：前端。验收：前端无 `tasks` 相关请求与页面入口。
- [ ] `P2-WI-QA` 完成迁移人工验收。角色：验收。验收：覆盖工作流型启动、节点推进、运行中加减设备、停止、并存占用一致、无孤立实例、手工任务入口已下线。

周期验收门禁：用户确认工作流型计划任务可直接创建、启动、运行中控制，且不再产生 `plan_run_id=0` 孤立实例；旧“工作流实例”入口与手工任务入口均已下线。

## 9. `[ ]` S3：最小后台与排障能力

阶段目标：人可以通过后台看设备、下发任务、查看基础日志和结果。

### `[ ]` C3-1：设备绑定与任务详情

验收文档：待实现后生成 `repos/mobilerpa-center/docs/web/前端手工验收.md`，并按需补充中心服务接口验收。

- [ ] `P3-BE-001` 实现设备绑定接口。角色：后端。验收：后台可提交 `physical_slot` 和 `group_name`。
- [ ] `P3-BE-004` 实现任务详情接口。角色：后端。验收：可查询任务事件、结果和错误摘要。
- [ ] `P3-FE-001` 实现设备绑定表单。角色：前端。验收：后台可录入物理位置和分组。
- [ ] `P3-FE-002` 实现任务创建表单。角色：前端。验收：后台可选择设备、脚本和参数创建任务。
- [ ] `P3-FE-003` 实现任务详情页。角色：前端。验收：后台可查看任务事件和结果。

周期验收门禁：用户确认设备可以录入物理位置和分组，任务详情可查看。

### `[x]` C3-1B：局域网设备发现与网页批量下发 Agent

验收文档：待实现后补充到 `repos/mobilerpa-center/docs/server/中心服务手工验收.md` 与 `repos/mobilerpa-center/docs/web/前端手工验收.md`。

- [~] `P3-BE-010` 设计并实现基于本机 `adb` 的局域网设备发现服务。角色：后端。验收：中心服务可从本机 `adb` 输出中识别已开启无线调试的局域网设备。
- [~] `P3-BE-011` 实现设备发现列表接口。角色：后端。验收：`GET /api/v1/discovery/devices` 可返回发现结果、连接状态和错误信息。
- [x] `P3-BE-012` 实现网页触发批量下发 Agent 接口。角色：后端。验收：`POST /api/v1/discovery/agent-deployments` 可接收多设备请求并返回逐设备结果。
- [ ] `P3-BE-013` 增加中心服务侧 `adb` 工具路径、Agent 仓库路径和默认中心地址配置项。角色：后端。验收：中心服务无需手工改代码即可调整发现与下发依赖路径。
- [ ] `P3-FE-010` 新增“设备发现”菜单入口和页面骨架。角色：前端。验收：左侧菜单出现“设备发现”，页面可进入。
- [x] `P3-FE-011` 实现设备发现列表、多选框和批量下发表单。角色：前端。验收：页面可勾选多台设备并填写中心地址发起下发。
- [ ] `P3-FE-012` 展示逐设备下发结果和失败原因。角色：前端。验收：页面可看到每台设备的连接、推送和启动结果。
- [x] `P3-QA-007` 编写设备发现与网页下发 Agent 的人工验收文档。角色：验收。验收：文档包含可直接操作的网页步骤、接口示例和失败排查方式。

周期验收门禁：用户确认网页可以发现局域网无线调试设备，并可对勾选设备完成 Agent 下发。

### `[ ]` C3-2：日志与截图排障链路

验收文档：待实现后补充到 `repos/mobilerpa-center/docs/server/中心服务手工验收.md`、`repos/mobilerpa-agent/docs/手机端手工验收.md` 和前端验收文档。

- [ ] `P3-BE-002` 实现基础日志接收。角色：后端。验收：设备可上传任务日志。
- [ ] `P3-BE-003` 实现截图上传。角色：后端。验收：设备可上传截图文件。
- [ ] `P3-BE-005` 实现文件路径归档规则。角色：后端。验收：日志和截图按日期、设备、任务落盘。
- [ ] `P3-AG-001` 实现日志上报。角色：Agent/脚本。验收：Agent 可上报脚本关键日志。
- [ ] `P3-AG-002` 实现异常截图。角色：Agent/脚本。验收：脚本失败时能生成并上传截图。
- [ ] `P3-FE-004` 实现日志查看入口。角色：前端。验收：后台可查看任务基础日志。
- [ ] `P3-FE-005` 实现截图查看入口。角色：前端。验收：后台可打开任务相关截图。

周期验收门禁：用户确认失败任务可以通过日志和截图定位问题。

### `[ ]` C3-3：多设备联调与脚本模块迁移

验收文档：待用户确认验收记录目录后生成小批量试运行记录。

- [ ] `P3-AG-003` 迁移 `shoppe` 关键页面模块。角色：Agent/脚本。验收：主流程可识别关键页面。
- [ ] `P3-AG-004` 建立本地调试入口。角色：Agent/脚本。验收：不依赖中心也可运行 debug 入口。
- [ ] `P3-QA-001` 完成 3 台设备联调。角色：验收。验收：3 台设备可注册、上线、接任务。
- [ ] `P3-QA-002` 完成 10 台设备联调。角色：验收。验收：10 台设备可稳定执行一轮任务。
- [ ] `P3-QA-003` 完成失败排障验收。角色：验收。验收：失败任务能通过日志和截图定位。
- [ ] `P3-QA-004` 记录第 3 阶段验收结论。角色：验收。验收：记录多机问题、脚本问题和后台问题。

周期验收门禁：用户确认 3 台到 10 台设备联调通过，并确认排障链路可用。

## 10. `[ ]` S4：早期生产试运行

阶段目标：支撑 10 到 30 台设备进入早期生产试运行。

### `[ ]` C4-1：稳定性基础补强

验收文档：待实现后补充到对应模块手工验收文档。

- [ ] `P4-BE-001` 完善断线重连状态处理。角色：后端。验收：设备断线重连后状态恢复正常。
- [ ] `P4-BE-002` 完善任务失败状态。角色：后端。验收：失败任务有明确错误码和错误描述。
- [ ] `P4-BE-003` 增加基础重试策略。角色：后端。验收：允许人工或系统重试失败任务。
- [ ] `P4-BE-004` 增加日志和截图清理策略。角色：后端。验收：可按日期清理临时文件。
- [ ] `P4-AG-001` 完善 Agent 重连策略。角色：Agent/脚本。验收：网络恢复后 Agent 自动重连。
- [ ] `P4-AG-002` 完善脚本失败证据链。角色：Agent/脚本。验收：失败时至少有步骤、错误、日志、截图。

周期验收门禁：用户确认断线、失败、重试、证据链等基础稳定性能力可接受。

### `[ ]` C4-2：早期生产入口与后台可操作性

验收文档：待实现后补充到对应模块手工验收文档。

- [ ] `P4-AG-003` 完成 `shoppe` 早期生产入口。角色：Agent/脚本。验收：可在测试设备组连续运行。
- [ ] `P4-AG-004` 输出脚本回滚说明。角色：Agent/脚本。验收：出现问题时可退回上一版本。
- [ ] `P4-FE-001` 优化设备状态展示。角色：前端。验收：在线、离线、执行中、异常状态清晰可见。
- [ ] `P4-FE-002` 优化任务状态展示。角色：前端。验收：任务成功、失败、执行中、等待状态清晰可见。
- [ ] `P4-FE-003` 增加基础错误提示。角色：前端。验收：接口失败时页面有明确反馈。
- [ ] `P4-FE-004` 支持试运行问题定位。角色：前端。验收：能从设备页或任务页进入日志和截图。

周期验收门禁：用户确认后台可用于早期生产的日常查看、下发、定位和回滚说明。

### `[ ]` C4-3：10 到 30 台试运行

验收文档：待用户确认验收记录目录后生成 10 到 30 台试运行记录。

- [ ] `P4-QA-001` 建立测试设备组。角色：验收。验收：测试设备有明确分组和编号。
- [ ] `P4-QA-002` 完成 10 台试运行。角色：验收。验收：10 台设备完成一轮真实任务。
- [ ] `P4-QA-003` 完成 30 台试运行。角色：验收。验收：30 台设备完成一轮真实任务。
- [ ] `P4-QA-004` 输出早期生产上线判断。角色：验收。验收：明确是否可以进入早期生产。
- [ ] `P4-QA-005` 输出上线问题清单。角色：验收。验收：问题按严重级别排序并指定负责人。

周期验收门禁：用户确认 10 到 30 台真实或接近真实设备试运行结果，并明确确认是否进入早期生产。

## 11. `[ ]` S5：规模化与生产加固

阶段目标：早期生产稳定后，继续向 50 到 100 台、再到 300 台扩容。

### `[ ]` C5-1：版本、灰度与异常中心

验收文档：待实现后补充到对应模块手工验收文档。

- [ ] `EXT-001` 完整脚本版本管理。角色：后端/前端。验收：支持上传、标记、切换和回滚版本。
- [ ] `EXT-002` 灰度发布。角色：后端/前端。验收：支持按设备组发布 beta 版本。
- [ ] `EXT-003` 异常中心。角色：后端/前端。验收：可按错误码、设备、任务筛选异常。
- [ ] `EXT-004` 日报中心。角色：后端/前端。验收：可查看每日成功率、失败分布和设备产出。
- [ ] `EXT-014` 规划网页查看脚本文件内容与在线编辑后另存为新版本。角色：后端/前端。验收：文档明确“只读查看”“编辑后必须生成新版本”“禁止直接改写已发布版本”等边界，并完成页面与接口设计。

周期验收门禁：用户确认版本切换、灰度发布、异常查看和日报能力达到生产管理要求。

### `[ ]` C5-2：文件归档与调试工具

验收文档：待实现后补充到对应模块手工验收文档。

- [ ] `EXT-005` 文件归档自动化。角色：后端。验收：支持定期归档和清理日志、截图、dump。
- [ ] `EXT-006` Go CLI 调试工具。角色：后端/Agent。验收：支持 push、run、tail、pull-screenshot。
- [ ] `EXT-011` 面向中心在线设备的批量 Agent 下发。角色：后端/Agent/前端。验收：后台可针对中心在线设备批量选择目标并发起 Agent 下发，不依赖本地 ADB 逐台操作。
- [ ] `EXT-012` Agent 版本与批量下发记录页。角色：后端/前端。验收：可查看 Agent 版本、批量下发任务、设备成功失败明细和重试结果。
- [ ] `EXT-013` 补齐 `mobilerpa-toolkit` 帮助文档。角色：后端/Agent。验收：`repos/mobilerpa-toolkit/README.md` 可独立说明构建方式、子命令、参数、典型使用示例与常见问题。

周期验收门禁：用户确认文件归档、清理和调试工具可以降低人工排障成本。

### `[ ]` C5-3：100 台到 300 台试运行

验收文档：待用户确认验收记录目录后生成 100 到 300 台试运行记录。

- [ ] `EXT-007` 100 台试运行。角色：验收。验收：100 台设备完成连续业务周期。
- [ ] `EXT-008` 300 台试运行。角色：验收。验收：300 台设备完成分组试运行。

周期验收门禁：用户确认 100 台试运行通过，并最终确认 300 台分组试运行通过。

## 12. 当前下一步建议

建议马上推进：

- [x] `NEXT-005` 完成脚本完整目录分发闭环。角色：后端/Agent。对应任务：`P2-BE-009`、`P2-BE-010`、`P2-AG-007`、`P2-AG-008`。验收：删除手机本地脚本版本目录后，下发任务可重新下载完整目录。
- [x] `NEXT-006` 完成脚本真机直跑调试入口。角色：Agent/脚本。对应任务：`P2-AG-009`。验收：AutoJs6 可直接运行 `index_debug.js` 调试 `shoppe_sync@v0.1.1`。
- [x] `NEXT-007` 建立脚本管理接口与页面。角色：后端/前端。对应任务：`P2-SC-001` 到 `P2-SC-010`。验收：页面可查看脚本版本、上传脚本并独立下发到手机。
- [x] `NEXT-008` 改造任务创建表单选择脚本版本。角色：前端。对应任务：`P2-SC-011`。验收：创建任务时从脚本管理数据中选择脚本版本。
- [x] `NEXT-009` 完成关键步骤事件上报。角色：后端/Agent/前端。对应任务：`P2-AG-006`。验收：单个任务执行时可看到关键步骤事件，中心可保存，后台可查看。
- [ ] `NEXT-010` 进入计划任务调度层基础设施建设。角色：后端。对应任务：`P2-PL-001` 到 `P2-PL-010`。验收：先完成计划任务主模型、时间规则和运行实例模型，再进入执行链迁移。
- [ ] `NEXT-011` 移除工作流实例层与手工任务、合并执行域。角色：后端/Agent/前端。对应周期：`C2-7`（任务 `P2-WI-001` 到 `P2-WI-QA`）。验收：执行域收敛为 `plan_def -> plan_run -> plan_device_run`，工作流型计划任务运行态由计划任务实例承载，手工任务入口下线，不再产生 `plan_run_id=0` 孤立实例。

按当前阶段顺序，你已确认“计划任务层”作为正式调度模型成立；下一步先按 `C2-6` 收口计划任务基础设施与运行实例，再按 `C2-7` 移除工作流实例层与手工任务、把工作流执行态并入计划任务实例，最终形成单一执行链路。
